export enum Culture {
  NL = 'nl-NL',
  EN = 'en-US',
  DE = 'de-DE',
  FR = 'fr-FR',
}

export enum FilterType {
  CHECKBOX = 'checkbox',
  RANGE = 'range',
  SELECT = 'select',
}

export enum ProductStatus {
  ACTIVE = 'active',
  OUTDATED = 'outdated',
  DRAFT = 'draft',
}
